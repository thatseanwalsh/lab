# secrets/cloudflare.env.template — copy to secrets/cloudflare.env and fill
# This file contains the Cloudflare API token used for DNS challenge.
# After filling, encrypt with sops and remove the plaintext before committing.

CLOUDFLARE_API_TOKEN=REPLACE_WITH_CLOUDFLARE_API_TOKEN
